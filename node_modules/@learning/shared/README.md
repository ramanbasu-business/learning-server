# learning-shared

Shared package for types and utilities used by the client and server repos.

## Build

```bash
npm install
npm run build
```

## Publish

Publish this package as `@learning/shared` to your package registry, then install it from the client and server repos using the published version.
